[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"]

[phases.install]
cmds = ["cd faq-backend && python -m pip install -r requirements.txt"]

[start]
cmd = "cd faq-backend && python app.py"

[variables]
PYTHONPATH = "faq-backend"
